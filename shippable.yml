# Language setting
language: node_js

# Version number
node_js:
  - "5.3"

build:
  pre_ci_boot:
    image_name: drydock/u14nod
    image_tag: prod
    pull: false

  ci:
    - node --version
    - pip install awsebcli
  post_ci:
    - cd /root/src/github.com/ttrahan/node-express-eb
    - eb deploy
